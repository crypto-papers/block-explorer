import React from 'react';

import '../../style/globals.scss';

const BlockGrid = () => (
  <div className="container">
    <h3 className="container-title">Latest Blocks:</h3>
  </div>
);

BlockGrid.displayName = 'BlockGrid';

export default BlockGrid;
